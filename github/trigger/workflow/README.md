# Ritchie Formula

## Premisses

- Github Account (with generated TOKEN with repository accesses)
- You will need to set Github credentials when executing the formula or through the `rit set credential` command first.

## Command

```bash
rit github trigger workflow
```

## Description

Formula to create through a command line a `repository_dispatch` or a `workflow_dispatch` event, with or without inputs.

## Demo

### Repository Dispatch without input

### Repository Dispatch with input (`client_payload`)

### Workflow Dispatch without input

### Workflow Dispatch with input
